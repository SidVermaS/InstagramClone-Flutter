import 'package:flutter/material.dart';

class SelfProfile extends StatelessWidget{
  Widget build(BuildContext context)  {
    return Scaffold(
      body: Center(child: Text('SelfProfile'))
    );
  }
}